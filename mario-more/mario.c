#include <cs50.h>
#include <stdio.h>

int main(void)
{
 do
 {
    int rows = get_int("input number of rows between 1 and 8:");
 }
 while (rows <1) || (rows>8)
 for (int counter=0; counter < rows; counter++ )
   
}